namespace Test/Exp/Concurrency/NewChannel {

    @test
    def testNewChannel01(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel02(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel03(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel04(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel05(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel06(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel07(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel08(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel09(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel10(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel11(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel12(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel13(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel14(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel15(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel16(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel17(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel18(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel19(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel20(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel21(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel22(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel23(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel24(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel25(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel26(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel27(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel28(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

    @test
    def testNewChannel29(): Unit \ IO = region _r { discard Channel.unbuffered(Static) }

    @test
    def testNewChannel30(): Unit \ IO = region _r { discard Channel.buffered(Static, 1) }

}
