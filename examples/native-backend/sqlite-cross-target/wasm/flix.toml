[package]
name = "sqlite-wasm"
description = "Wasm SQLite bridge example."
version = "0.1.0"
flix = "0.68.0"
authors = ["The Flix Team"]
license = "Apache-2.0"

[build]
targets = ["wasm"]

[[bindings.wasm]]
wit = "../contracts/wit"
world = "sqlite-demo"
module = "Wit"
