Concerns:
    empty types a la Expression[PRefType.PUnit].


Questions:

    How to make RETURN?
        def RETURN[R <: Stack]: F[StackNil / R] => F[Nothing / End] =

        ... F[StackNil] => F[End]

        ANS:
        ARETURN etc.

    How is string stored in function builder?

    How do you use function builder when no value has type void?
        trait FunctionBuilder[T <: PType]
        object FunctionBuilder {
            case object Void extends FunctionBuilder[PVOID]
            case object Int32 extends FunctionBuilder[PInt32]
        }

    På typestack'en - ligger der PRef[PReference[PChan[PInt32]]]
        selvom det egentlig er PRef[PAnyObject]?

    Scenario - PRef constructor
        aload(0) : pusher this
        Hvad ligger på jvm stakken? object af type Ref$Obj
        Hvad ligger på type stakken? PRef[PChan[PInt32]]
        hvis ja - PRef[PChan[PInt32]] er ikke over approx af
            Ref$Obj,men mere specific

    Related - does current pushNull make sense?


jvm:
    Bool array used as byte in operations.

    reference types:
        null value, can be all reference types.

        class:
            instances on the heap.
        interface:
        array:
            instances on the heap.
            of the form: array[int], array[array[class], etc.

    stack shit:
        received arguments are by local variables
            0: this
            1: arg1
            2: arg2
            ...
        operand stack is initially empty
        "Each entry on the operand stack
            can hold a value of any Java Virtual
            Machine type, including a value of
            type long or type double."
        stack depth counts size as if stored in local variables
        dup/swap are the only untyped operations
            (cant break cat2 values)

    constructor:
        defined in a class.
        has name <init>
        returns void
        only called by invokespecial instruction

    class constructor
        defined in class or interface
        has name <clinit>
        no arguments
        returns void
