mod Test.Unused.Def {

    // NB: Compile, but not execute.
    def _unusedDef01(): Int32 =
        ?hole

    // NB: Compile, but not execute.
    // Unused math name
    def _√(): Int32 =
        ?hole

    // NB: Compile, but not execute.
    // Unused greek name
    def _Χαίρετε(): Int32 =
        ?hole

    // NB: Compile, but not execute.
    // Unused operator name
    def _>==>(): Int32 =
        ?hole
}
