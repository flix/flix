Type LocalVar, Statement.

// input facts.
Relation DefSite(s: Statement, l: LocalVar).
Relation UseSite(s: Statement, l: LocalVar).

Relation Successor(s1: Statement, s2: Statement).

// liveness analysis
Relation LiveIn(s: Statement, l: LocalVar).
Relation LiveOut(s: Statement, l: LocalVar).

Constraint LiveIn(s, l) :- UseSite(s, l).
Constraint LiveOut(s, l) :- LiveIn(s, l), !DefSite(s, l).
Constraint LiveOut(s2, l) :- LiveIn(s1, l), Successor(s1, s2).
