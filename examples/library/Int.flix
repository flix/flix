namespace Int {

    // TODO: Would this be useful to have?

    fn minValue(): Int = extern

    fn maxValue(): Int = extern

    fn min(x: Int, y: Int): Int = extern

    fn max(x: Int, y: Int): Int = extern

    // or alternatively.

    extern fn toChar(x: Int): Char

}