namespace Test/Unused/Tag {

    enum Color {
        case _Red,
        case _Blu
    }

    enum Currency {
        case USD,
        case _CAD,
    }

    def f(): Color = ???

    def g(): Currency = USD

}
