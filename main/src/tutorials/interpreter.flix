//
// In this tutorial we demonstrate how to use the
// functional language of Flix to write a small interpreter.
//
namespace interpreter {

    enum Expr {
        case Plus(Expr, Expr)
    }



    rel R(x: Int)


}