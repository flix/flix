/**
 * Types equipped with an equality relation.
 */
class Eq[E] {

    /**
     * Returns `true` if `x` is equal to `y`.
     */
    def eq(x: E, y: E): Bool

    // TODO: Alias ==?

    // TODO:  Alias ≡??

    // TODO: Laws


}
